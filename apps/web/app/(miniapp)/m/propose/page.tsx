// /m/propose — chat-like NL event submission flow.

import { ProposeFlow } from "./ProposeFlow";

export const dynamic = "force-dynamic";

export default function ProposePage() {
  return <ProposeFlow />;
}
